<form method="post"
      action="{{ path('app_categoria_delete', {id: categoria.id}) }}"
      onsubmit="return confirm('¿Seguro que quieres eliminar esta categoría?');">

    <input type="hidden" name="_token"
           value="{{ csrf_token('delete' ~ categoria.id) }}">

    <button class="admin-delete">Eliminar</button>
</form>

